{
  "name": "trading-bot",
  "version": "1.0.0",
  "engines": {
    "node": "18.17.0"
  },
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  }
}
